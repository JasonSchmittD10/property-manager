# These MUST match the values in src/config/property.ts → appAccess
# (the auth gate reads these env vars; appAccess is for displaying the
# credentials inside the Property tab for tenants who forget).
VITE_APP_USERNAME=bashfordcrest
VITE_APP_PASSWORD=2026
